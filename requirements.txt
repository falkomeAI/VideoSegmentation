# Core dependencies
PyQt6>=6.4.0
opencv-python>=4.8.0
numpy>=1.24.0

# PyTorch (CPU version - for GPU install pytorch with CUDA)
torch>=2.0.0
torchvision>=0.15.0

# SAM2 (install separately)
# git clone https://github.com/facebookresearch/sam2.git
# cd sam2 && pip install -e .
